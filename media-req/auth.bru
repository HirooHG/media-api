meta {
  name: auth
  type: http
  seq: 6
}

post {
  url: {{API_URI}}/auth
  body: json
  auth: inherit
}

body:json {
  {
    "username": "hiroo",
    "password": "{{ADMIN_PWD}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
