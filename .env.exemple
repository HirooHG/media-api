PORT=3000
ORIGIN=http://localhost:$PORT
NODE_ENV=dev

DB_HOST=localhost

DOMAIN=art
TOKEN=example
IDENTITY=example

HASH_ALGO=argon2id
JWT_SECRET=<32 bit hexa rand>
JWT_ALGO=RS256
ADMIN_NAME=TO_CHANGE
ADMIN_PWD=TO_CHANGE

# minio
MINIO_BUCKET="TO_CHANGE"
MINIO_ENDPOINT="TO_CHANGE"
MINIO_PORT="TO_CHANGE"
#########################
## minio
#########################
MINIO_ROOT_USER="TO_CHANGE"
MINIO_ROOT_PASSWORD="TO_CHANGE"

#########################
## keycloak
#########################
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin
KEYCLOAK_CLIENT_SECRET=<32 bit hexa rand>
KC_PORT=8080
KC_HOSTNAME=http://localhost:$KC_PORT

KC_DB_URL=jdbc:postgresql://db/postgres
KC_DB=postgres
KC_DB_USERNAME=postgres
KC_DB_PASSWORD=postgres
