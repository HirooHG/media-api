#########################
## API
#########################
PORT=3000
WS_PORT=3002
ORIGIN=http://localhost:$PORT
NODE_ENV=dev

DB_HOST=localhost:27017

DOMAIN=com
TOKEN=example
IDENTITY=example

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=example
KEYCLOAK_CLIENT_ID=example
KEYCLOAK_STORE_SECRET=example

# minio
MINIO_BUCKET=example
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
#########################
## minio
#########################
MINIO_ROOT_USER=example
MINIO_ROOT_PASSWORD=example

#########################
## keycloak
#########################
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin
KEYCLOAK_CLIENT_SECRET=<32 bit hexa rand>
KC_PORT=8080
KC_HOSTNAME=http://localhost:$KC_PORT

KC_DB_URL=jdbc:postgresql://db/postgres
KC_DB=postgres
KC_DB_USERNAME=postgres
KC_DB_PASSWORD=postgres
