PORT=3000
ORIGIN=http://localhost:$PORT
NODE_ENV=dev

DB_HOST=localhost

DOMAIN=art
TOKEN=example
IDENTITY=example

HASH_ALGO=argon2id
JWT_SECRET=<32 bit hexa rand>
JWT_ALGO=RS256
ADMIN_NAME=TO_CHANGE
ADMIN_PWD=TO_CHANGE
